{
  "name": "nest-enum-escaping",
  "version": "1.0.0",
  "description": "Issue with nest escaping values inside enum.",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "start": "ts-node -r tsconfig-paths/register src/main.ts",
    "start:dev": "tsc-watch -p tsconfig.build.json --onSuccess \"node dist/src/main.js\""
  },
  "author": "Duckies",
  "license": "ISC",
  "dependencies": {
    "pg": "^7.12.1",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^3.0.0",
    "typeorm": "^0.2.25"
  },
  "devDependencies": {
    "@types/node": "^12.7.2",
    "ts-node": "^8.3.0",
    "tsc-watch": "^2.4.0",
    "tsconfig-paths": "^3.8.0",
    "typescript": "^3.5.3"
  }
}
